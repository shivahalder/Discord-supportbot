# nixpacks.toml for Discord bot — simple version

[phases.setup]
nixPkgs = ["python3"]
aptPkgs = ["curl", "wget"]

[phases.install]
commands = [
  "pip install --upgrade pip",
  "pip install --no-cache-dir -r requirements.txt"
]

[phases.start]
commands = [
  "python3 support_bot.py"
]
